import React, { Component } from "react"
import styled from "styled-components"

export default class ExperienceList extends Component {
  render() {
    return <ExperienceListWrapper />
  }
}

const ExperienceListWrapper = styled.div``
